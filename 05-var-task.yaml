#variables at task-level
- name: var for tasklevel
  hosts: localhost
  vars:
   course: "devsecops"
   trainer: "raveen"
   duration: "4months"
  tasks:
  - name: check var task level
    vars:
       course: "devops with aws "
       trainer: "shiva"
       duration: "110"
    ansible.builtin.debug:
       msg: "hi im learning {{course}}, trainer is {{trainer}}, duration is {{duration}}"